import React from 'react';
import ShowProducts from '../components/ShowProducts';

const Products = () => {
    return (
        <div>
            <ShowProducts/>
        </div>
    );
};

export default Products;